# Logs

This folder stores performance logs (CSV) for the Year 1 feasibility study.

- `example_latency_laptop.csv`: Representative log collected on a laptop webcam run (100 frames).
  Columns:
  - frame
  - latency_ms
  - cpu_percent
  - ram_mb

Note:
- Other generated CSV logs are ignored by git to keep the repo clean.
