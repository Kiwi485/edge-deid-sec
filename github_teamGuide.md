# GitHub 協作指南（團隊版）

本文件說明本專案在 GitHub / Git 上的協作方式、分支規範、  
Pull Request（PR）流程，以及多人合作時的常見問題與建議做法。

請所有成員在開始開發前閱讀本文件，並於整個專案期間遵守。

---

## 🎯 本指南的目標

- 確保 `main` 分支永遠穩定、可執行
- 避免多人同時開發造成衝突
- 讓每個功能、修改都有清楚的紀錄
- 讓新成員能快速理解團隊 Git 流程

---

## 🌳 分支規則（核心原則）

### 分支角色說明

- `main`
  - 穩定分支
  - ❌ **禁止直接開發**
  - 只能透過 Pull Request 合併

- `feature-*`
  - 個人或功能開發分支
  - 每位成員只能在自己的分支上開發

### 分支命名建議

```text
feature-login
feature-roi-detection
feature-schedule
fix-camera-bug
docs-update-readme
````

---

## 👥 新成員開始流程

### 1️⃣ Clone 專案

```bash
git clone https://github.com/Kiwi485/edge-deid-sec.git
cd edge-deid-sec
```

---

### 2️⃣ 從 main 建立自己的分支

```bash
git checkout main
git pull origin main
git checkout -b feature-你的功能名稱
```

---

## 🔁 日常開發流程（每次都照這樣）

### 修改程式碼後

```bash
git status
git add .
git commit -m "清楚描述你做了什麼"
git push origin feature-你的功能名稱
```

### Commit 訊息建議

* ✅ 好的範例：

  * `feat: add hand landmark ROI extraction`
  * `fix: camera index handling on Windows`
  * `docs: update setup instructions`

* ❌ 避免使用：

  * `update`
  * `fix`
  * `test`

---

## 🔀 Pull Request（PR）流程

### 什麼時候要發 PR？

建議以下時機發 PR：

1. **功能完成時**

   * 例如：一個模組或一個功能寫完

2. **功能已可執行時**

   * 即使尚未 100% 完成，但能跑、能測試

3. **文件或小修改**

   * README、Setup、typo 修正

---

### PR 建立流程

1. 將分支 push 到 GitHub
2. 在 GitHub 建立 Pull Request
3. 設定：

   * `base`：`main`
   * `compare`：你的分支
4. 簡要說明你做了什麼
5. 等待至少一位隊友 review

❗ **未經 review，請勿自行合併 `main`**

---

## 🔄 同步最新 main（避免衝突）

在開始新功能前，請先更新 main：

```bash
git checkout main
git pull origin main
git checkout feature-你的分支
git merge main
```

若發生衝突，請先解決後再繼續開發。

---

## 👀 查看隊友的程式碼

### 方法一（推薦）：看 GitHub 上的 PR

* 可直接看到紅 / 綠差異
* 可留言討論修改

---

### 方法二：切換到隊友分支

```bash
git fetch origin
git checkout feature-隊友的分支
```

切回自己分支：

```bash
git checkout feature-你的分支
```

---

### 方法三：比較差異

```bash
git diff main..feature-隊友的分支
```

---

## ⏪ 回到舊版本（進階，請小心）

### 暫時查看某個 commit

```bash
git checkout <commit_id>
```

### 從舊版本建立新分支

```bash
git checkout -b old-feature <commit_id>
```

### 強制回退（⚠️ 會刪除歷史）

```bash
git reset --hard <commit_id>
```

⚠️ 使用前請先確認，或詢問隊友。

---

## ❓ 常見問題 Q&A

### Q1：`git fetch origin` 是什麼？

只更新遠端分支資訊，不會改你本地檔案。

---

### Q2：merge 和 commit 有什麼差？

* `commit`：儲存自己的修改
* `merge`：把別人的分支合進來

---

### Q3：切到隊友分支後，怎麼回自己分支？

```bash
git checkout feature-我的分支
```

---

### Q4：還沒 commit 但想切分支怎麼辦？

```bash
git stash
git checkout feature-其他分支
git checkout feature-我的分支
git stash pop
```

---

## 📦 Git 常用指令速查

```bash
git status
git log --oneline
git add .
git commit -m "訊息"
git push origin <branch>
git pull origin main
```

---

## ⚠️ 團隊注意事項（請遵守）

* 禁止直接在 `main` 分支開發
* PR 未 review 不得合併
* 請勿將以下內容加入 Git：

  * `.venv / .venv311`
  * `__pycache__`
* 不確定操作時，請先詢問

---

## 🧠 團隊共識（請記住）

> main 不動
> 功能開分支
> 改完發 PR
> review 才合併


