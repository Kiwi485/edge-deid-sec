# Python virtual environments
.venv/
.venv311/
venv311/
# Python cache
__pycache__/
*.pyc

# VS Code / editor
.vscode/

# OS
.DS_Store

logs/*.csv
!logs/example_latency_laptop.csv
!logs/example_latency_vm.csv

# generated image/data

data/out/

# auto-generated reports (changes every test run)
docs/roi_eval.md

# keep optional placeholders
!data/out/.

# evidence 
evidence

#raw/txt（原本為避免資料太大而忽略，若需將標註結果版本控，可將下列註解）
# data/raw/*.txt
# data/verify_out

# 目前專案需要追蹤的標註與可視化結果
!data/raw/*.txt
!data/verify_out/**
!evidence/w4-issue2/**

# 其他不需要追蹤的資料夾或檔案
results/benchmark_vm_v0_check.md
results/benchmark_vm_v0.md

# ── Tongue Segmentation (src/seg/) ──────────────────────────────────

# CVAT / Roboflow 匯入的原始影像（體積大，不放 git）
data/cvat/
# 但保留 CVAT 匯出的標註 JSON（讓 git 追蹤標註版本）
!data/cvat/**/annotations/
!data/cvat/**/annotations/*.json


# 訓練產出的 model checkpoint（體積大）
models/

# 推論輸出圖片
outputs/

# 訓練時產生的 Python cache（seg 模組）
src/seg/__pycache__/

viz_unet_mobilenet/

roi_face_test_out/
roi_seg_test_out/ 

test_roi_face.py
# ── YOLO ROI detector ─────────────────────────────────
runs/
data/yolo/
yolov8n.pt
*.cache

# pytest
.pytest_cache/

# 保留訓練好的 YOLO 權重（讓隊友直接用，不必重訓）
!models/yolo/
!models/yolo/best.pt

# iPhone HEIC 原檔（pipeline 不支援，請先轉成 .jpg）
data/raw/*.HEIC
data/raw/*.heic

# ── data/raw 新增圖片不追蹤（只保留 .txt 標註） ──
# 已經 commit 過的舊圖會繼續被追蹤，只有「未被追蹤的新圖」會被擋下
data/raw/*.jpg
data/raw/*.JPG
data/raw/*.jpeg
data/raw/*.JPEG
data/raw/*.png
data/raw/*.PNG
# .txt 標註保留（前面已經有 !data/raw/*.txt 例外）

docs/.edge-pdf-profile
docs/.edge-pdf-profile-figs
docs/.edge-preview-profile
docs/paper_draft_ieee_with_figures.pdf

# ── experiments directory ─────────────────────────────────────────────
# Source code under experiments/ IS tracked (e.g. experiments/acm_paper/).
# Only ignore generated experiment artifacts — those already go to outputs/
# and models/ which are gitignored above.
#
# The old blanket rule "experiments" has been removed so that new source
# files committed under experiments/acm_paper/ are properly tracked.
experiments/unet_backbone_compare/saved_models/
experiments/unet_backbone_compare/runs/

# ── ACM paper experiments ─────────────────────────────────────────────
# outputs/acm_paper/ and models/acm_paper/ are already covered by the
# blanket outputs/ and models/ ignore rules above.
# The source code under experiments/acm_paper/ IS tracked.

# Block stray YOLO base-model downloads in the project root
yolov8n-seg.pt

# Allow RQ1 outputs
!outputs/acm_paper/rq1/split_manifest.json
!outputs/acm_paper/rq1/*.json
!outputs/acm_paper/rq1/figures/